<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Payment Successful - MD College</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/images/mdcLogo.png" rel="icon">
    <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Heebo', sans-serif;
        }

        .success-box {
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 40px;
            max-width: 650px;
            margin: 60px auto;
            text-align: center;
        }

        .success-icon {
            font-size: 60px;
            color: #28a745;
            animation: pop 0.6s ease;
        }

        @keyframes pop {
            0% {
                transform: scale(0.8);
                opacity: 0;
            }

            100% {
                transform: scale(1);
                opacity: 1;
            }
        }

        .details {
            text-align: left;
            margin-top: 30px;
        }

        .details li {
            margin-bottom: 10px;
            font-size: 16px;
        }

        .btn-home {
            margin-top: 30px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="success-box">
            <i class="fas fa-check-circle success-icon"></i>
            <h2 class="mt-4">Payment Successful!</h2>
            <p class="lead">Thank you, <strong><%= user.fullName %></strong>, for completing your payment.</p>
            <hr>
            <h4 class="text-success mb-3">Transaction Details</h4>
            <ul class="list-unstyled details">
                <!-- <li><strong>Transaction ID:</strong> <%# payment.transactionid %></li> -->
                <li><strong>Payment ID:</strong> <%= appliedUser.paymentId %></li>
                <!-- <li><strong>Order ID:</strong> <%# payment.orderid %></li> -->
                <li><strong>Payment Receipt:</strong> <%= appliedUser.receiptNo %></li>
                <!-- <li><strong>Amount Paid:</strong> ₹<%# payment.amount %></li> -->
                <li><strong>Amount Paid:</strong> ₹<%= appliedUser.admissionFee %></li>
                <!-- <li><strong>Payment Mode:</strong> <%# payment.payment_method_type.toUpperCase() %></li> -->
                <li><strong>Payment Mode:</strong> Online</li>
                <!-- <li><strong>Date:</strong> <%# payment.date %></li> -->
                <li><strong>Date:</strong> <%= appliedUser.dateAndTimeOfPayment %></li>
            </ul>

            <a href="/bca-2-24-27-adm-form" class="btn btn-success btn-home">Go to Dashboard</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>

