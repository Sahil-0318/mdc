<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Payment Successful - MD College</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <link href="/images/mdcLogo.png" rel="icon">
    <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .success-box {
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 40px;
            max-width: 600px;
            margin: 60px auto;
            text-align: center;
        }

        .success-icon {
            font-size: 60px;
            color: #28a745;
            animation: pop 0.6s ease;
        }

        @keyframes pop {
            0% {
                transform: scale(0.8);
                opacity: 0;
            }

            100% {
                transform: scale(1);
                opacity: 1;
            }
        }

        .btn-home {
            margin-top: 30px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="success-box">
            <i class="fas fa-check-circle success-icon"></i>
            <h2 class="mt-4">Payment Successful!</h2>
            <p class="lead">Thank you, <strong><%= user.userName %></strong>, for completing your admission fee payment.</p>
            <hr>
            <h4 class="text-success mb-3">Transaction Details</h4>
            <p><strong>Reference No:</strong> <%= user.referenceNumber %></p>
            <p><strong>Course:</strong> <%= user.course %></p>
            <p><strong>Mobile:</strong> <%= user.mobileNumber %></p>
            <p><strong>Amount Paid:</strong> ₹<%= appliedUser.admissionFee %> /-</p>

            <div class="alert alert-success mt-4">
                <!-- You will receive a confirmation SMS and email shortly. -->
            </div>

            <a href="/ug-reg-sem-1-25-29-adm-form" class="btn btn-success btn-home">Go to Dashboard</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
